FROM node:20-alpine
WORKDIR /app

# Install dependencies (including devDeps for ts-node + seed)
COPY package.json package-lock.json* ./
RUN npm install

# Copy source
COPY . .

# Generate Prisma client
RUN npx prisma generate

EXPOSE 3000
ENV NODE_ENV=development
CMD ["npm", "run", "dev"]
